import 'package:flutter/material.dart';

class MostSkipped extends StatelessWidget {
  const MostSkipped({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Most Skipped Medicines'),
    );
  }
}
